<div class="alert alert-primary" role="alert">
  <h3 class="text-center">
    {{campground.name}}
  </h3>

  <h4 class="text-center">
    {{campground.city}}{{join}}{{campground.state}}
  </h4>
</div>

{{yield}}
