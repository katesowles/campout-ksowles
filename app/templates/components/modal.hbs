{{!-- NOTE: Not sure why this integration test is failing...More research required --}}

<div id="formModal" class="modal" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">{{headerTitle}}</h5>
        <button {{action "closeModal"}} type="button" class="close" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body p-3">
        {{yield}}
      </div>
    </div>
  </div>
</div>
