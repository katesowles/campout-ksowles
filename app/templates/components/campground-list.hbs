<ul class="list">
  {{#each list as |campground|}}
    <li>
      {{#side-menu-link-to "detail" campground.id}}
        {{campground.name}}
      {{/side-menu-link-to}}
    </li>
  {{/each}}
</ul>

{{yield}}
